# @gameweave/bots

Faction-aware perception, visual and auditory stimuli, target selection, navigation adapters, state machines, and composable bot behaviors for GameWeave.

```ts
const game = createGame().use(bots());
bot.set(BotController, { behavior: "assault" });
```
