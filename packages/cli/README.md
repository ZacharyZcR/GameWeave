# @gameweave/cli

Build and platform export tools for GameWeave games.

```bash
gameweave build
gameweave export web
gameweave export windows
```

Use `gameweave.config.ts` in the project root to configure game metadata, build output, and desktop window settings.
